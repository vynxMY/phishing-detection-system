# Frontend (PSM Core)

The PSM web UI is **server-rendered Jinja templates** served by Flask:

```
backend/app/templates/
backend/app/static/
```

A separate React/TypeScript SPA (Post-PSM / extension roadmap) would live in this
`frontend/` directory later. Until then, do not duplicate UI here.
