# Optional worker image — not used in the current single-container Compose stack.
# Keep this stub so the repo matches the Master Technical Specification layout.
FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
CMD ["echo", "Worker image unused in current deployment; see docs/DEPLOYMENT.md"]
